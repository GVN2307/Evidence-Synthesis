rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Emergency Demo Mode: Allow all read/write access
      // In production, this would be restricted to authenticated users.
      allow read, write: if true;
    }
  }
}
